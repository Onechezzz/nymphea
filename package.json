{
  "name": "aroma",
  "version": "1.0.0",
  "description": "Perfume selection app with admin panel and quiz",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build:packages && concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:packages": "npm run build --workspace=@aroma/shared-types && npm run build --workspace=@aroma/matching-engine",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build": "npm run build --workspaces",
    "db:migrate": "npm run migrate --workspace=backend",
    "db:seed": "npm run seed --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
